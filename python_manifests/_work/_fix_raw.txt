====================================================================================================
FILES: python_linting_practices_manifest.md
APPLIED: 13 | REFUTED: 5 | MEASURED: 20 | ADDED: 15
  REFUTED: Finding 3 (target-version) prescribed `py312`; DECISIONS.md D1 mandates `py313` (hub §3e's recommended default is `requires-python = ">=3.13"`, verified present at python_platform_baseline_manifest.md:245-262). D1 wins — applied `target-version = "py313"`. The finding cited the older §3c/§3d floor t
  REFUTED: Finding 2's pep257 disable list included `D420` flat. Qualified, not refuted: MEASURED shows pep257 DOES disable D420, but only observably with `preview = true` (D420/D421 are the two preview `D` rules). The table says so.
  REFUTED: Finding 9's PREMISE that ruff's formatter-conflict list contains ISC001 is WRONG. https://docs.astral.sh/ruff/formatter/ (fetched 8 Aug 2026) lists only `ISC002`, and only "when used without `ISC001` and `flake8-implicit-str-concat.allow-multiline = false`". ISC001 is absent. The prescribed ACTION (
  REFUTED: DECISIONS.md D5c and finding 7 both say "fourteen documented conflicts". The formatter page bullets FIFTEEN codes (W191, E111, E114, E117, D203, D206, D300, Q000-Q004, COM812, COM819, ISC002), with E501 named separately under a best-effort caveat. Wrote "fifteen" and handled E501 separately.
  REFUTED: Finding 7 / D5c list only rules as unguarded. MEASURED shows the guard rail ALSO covers two *settings*: `isort.lines-after-imports` at a value other than -1/1/2, and `flake8-implicit-str-concat.allow-multiline = false` without ISC001; the other four isort settings on the docs' list are silent. State
  NOTES: OWNERSHIP (D3): everything this file owns is kept and now correct — the rule set, the 18 removed codes, the MEASURED prefix distribution, the suppression tables (§8.2-§8.3), and the pydocstyle convention mechanism with its measured disable lists (§6.3). Other fixers citing me can point at: §2.4 (target-version binding), §3.2 (family verdicts, now complete), §4.1/§4.2 (default status), §4.3 (the one config block, now measured-valid), §6.1 (the partial guard rail), §6.3 (the convention lists), §3.2's stale-configuration paragraph (D5a). §6.2 already cedes the mypy --strict inventory to python_typing_contract_manifest.md and names only the five flags its own overlap argument needs, so no cut wa
====================================================================================================
FILES: python_quality_gates_manifest.md
APPLIED: 8 | REFUTED: 2 | MEASURED: 9 | ADDED: 10
  REFUTED: None of the 8 findings was refuted. One prescription was SUPERSEDED by DECISIONS.md D3: finding 1 asked me to write the three corrected pydocstyle disable lists into gates §11.1, while D3 assigns those lists to python_linting_practices_manifest.md §6.3/§15.1 and tells gates to cut to a cross-referen
  REFUTED: Finding 1's prescribed pep257 list is right in ruff's DOCS but wrong at DEFAULT settings: D420 (`incorrect-section-order`) is a PREVIEW rule, so it is absent from the resolved pep257 disable set unless `lint.preview = true` (MEASURED, ruff 0.16.2). Not applied here since the lists left this file; fl
  NOTES: HANDOFF TO THE LINTING FIXER (important): gates §11.1 now points at python_linting_practices_manifest.md §6.3 and §15.1 for the pydocstyle disable lists. Those lists in the linting file are, per finding 2, the SAME wrong lists (with D216). My cross-reference is only correct once they are fixed there. The measured, correct sets are in `measured` above. Two refinements the auditor's docs-derived lists miss: (a) D203/D213 are in ruff's documented lists but a bare `select = ["D"]` already drops them via the D203/D211 and D212/D213 incompatibility resolution, so a reader who selects them by exact code keeps them under every convention; (b) D420 only enters the pep257 set under `lint.preview = tru
====================================================================================================
FILES: python_platform_baseline_manifest.md
APPLIED: 8 | REFUTED: 4 | MEASURED: 10 | ADDED: 15
  REFUTED: DECISIONS.md D1 (partial): D1 lists group-aware `contextlib.suppress` as one of four constructs that 3.13 'makes native'. FALSE — docs.python.org/3/library/contextlib.html reads 'Changed in version 3.12: suppress now supports suppressing exceptions raised as part of a BaseExceptionGroup'. I therefor
  REFUTED: Finding 4 (partial): the prescription 'change both line 288 and line 997 to `sentinel(name, /, *, repr=None)`' is right for the §4a ledger row but wrong for the Sources entry — that entry attributes the signature to PEP 661, and peps.python.org/pep-0661 literally writes `sentinel(name, /, repr=None)
  REFUTED: Finding 3 (partial): the prescribed 3.20 row says 'the __version__ / version / VERSION attributes of 25 stdlib modules' but the module list on docs.python.org/3.15/deprecations/index.html has 24 entries (argparse … zlib), and the auditor's own enumeration also lists 24. Row written as '24 stdlib mod
  REFUTED: Findings 1 and 5 also prescribe edits to `_work/facts/r01_platform_baseline.md` (lines 91 and 237). NOT DONE — fact packs are explicitly out of my edit scope. Both source errors remain in r01 and need a separate pass: :91 mis-files ALLOW_MISSING under 3.15, :237 carries the unsourced ~1,400 bugfix f
  NOTES: FOR THE THREE FILES CITING §3e: the anchor exists and decides. Cite it as "§3e" (section number unchanged, per D8.2); inside it the default lives under the heading "#### The recommended default: `requires-python = \">=3.13\"`". Exact strings you can quote: default `requires-python = ">=3.13"`; alternative `">=3.12"` (SPEC 0-aligned, forfeits the three 3.13 constructs plus ReadOnly/TypeIs/type-parameter defaults, and SPEC 0 drops 3.12 in Q4 2026); prefer `">=3.14"` where nothing forces wider compatibility. NOTE FOR THE LINTING FIXER (D1 last bullet): D1 sets `target-version = "py313"` to match the floor — consistent with what §3e now says; nothing in §3e contradicts it.  TAG LEGEND (D7): MEAS
====================================================================================================
FILES: python_module_boundaries_manifest.md
APPLIED: 6 | REFUTED: 3 | MEASURED: 8 | ADDED: 14
  REFUTED: F5 [MINOR] "seam is not in architecture_manifest_default.md §1" — REFUTED, no edit made. `grep -n -i seam architecture_manifest_default.md` returns line 24: `- **Seam.** A point in existing code where behavior can be altered` — i.e. the word IS defined in §1 Vocabulary (lines 15-47). The auditor's g
  REFUTED: F2 [MAJOR] deptry missing from the gate ledger — NOT APPLIED HERE, out of scope. Every prescribed edit (a §2.1 ledger row, a pinned-tool-table entry, a suppression note) lands in python_quality_gates_manifest.md, which is another fixer's file; my brief forbids touching it. My file's §12 item 8 and §
  REFUTED: F3's prescribed floor value `>=3.12` — NOT USED; DECISIONS.md D1 overrides it with `>=3.13` (the finding predates the decision). Both deferral sites now read "the floor comes from python_platform_baseline_manifest.md §3e — default `>=3.13` absent a stated policy".
  NOTES: Two cross-file dependencies this file now asserts and cannot satisfy alone: (1) §5.1 and §12 item 4 cite python_platform_baseline_manifest.md §3e for a `>=3.13` default — if the hub fixer does not add that default per D1, my file points at a table with no recommendation and the deferral is circular again; (2) §9.3 states that python_quality_gates_manifest.md §8.4 cites the four-part table rather than restating it — if that fixer keeps a three-part list, the collection contradicts itself in the opposite direction from before. F2 (deptry absent from the gate ledger) is untouched and still OPEN in python_quality_gates_manifest.md; my §12 item 8 continues to require `deptry .` on day one. python
====================================================================================================
FILES: python_language_hazards_manifest.md
APPLIED: 7 | REFUTED: 2 | MEASURED: 15 | ADDED: 8
  REFUTED: Finding 4 (partial refutation of the prescribed URL, not the substance): the prescription said to cite `https://import-linter.readthedocs.io/en/v2.13/contract_types.html`. I curl-tested it: HTTP 404. The 2.13 docs moved to mkdocs-material directory URLs, so the resolving path is `https://import-lint
  REFUTED: Finding 6 (prescription adjusted, not refuted): the prescribed text routes `D` to '§15 of the linting file'. Verified both candidate sections exist and both are load-bearing — `python_linting_practices_manifest.md` §15 'Docstrings as contracts' (L1275) and §6.3 `lint.pydocstyle.convention` (L510) — 
  NOTES: No conflict between DECISIONS.md and my findings file — D3 matched finding 3, D5a matched finding 1, D5b was additive to it, and I applied all three.  TWO CONSISTENCY EDITS BEYOND THE SEVEN FINDINGS, both forced by them: 1. TL;DR bullet 2 said 'a rule with no hazard row is noise'. That is the same false absolutism finding 6 attacks in §0.3, one screen earlier, so I weakened it to 'a rule with neither a hazard row nor a recorded noise/consistency judgement (§0.3)'. Flagging it because it was not in my findings file. 2. The `### Sibling manifests` entry for python_linting_practices_manifest.md now names the four things §1.1 stopped restating (18-code list, prefix distribution, preview trap, §2
====================================================================================================
FILES: python_testing_tooling_manifest.md
APPLIED: 5 | REFUTED: 3 | MEASURED: 10 | ADDED: 7
  REFUTED: None refuted. One prescription was applied in a corrected form: finding 4's suggested default text says '… and every test module basename unique (prepend mode plus duplicate basenames is the classic collection error)' as if uniqueness were required under the recommended import mode. MEASURED with py
  REFUTED: Finding 1's closing instruction ('either the hub states one collection-wide recommendation … or error_tracing §21 is re-tagged as an error-contract-local preference') is superseded by DECISIONS.md D1, which already chose the first branch. Applied D1: the floor claim is deleted, the hub owns the poli
  REFUTED: Finding 4's second prescribed edit — add "INP001" to the `tests/**` entry of `per-file-ignores` in python_linting_practices_manifest.md — was NOT made: that file is another fixer's assignment. Verified the gap is still open (line 366: `"tests/**" = ["S101", "PLR2004", "D103"]`, and `"INP"` is in `se
  NOTES: OPEN for the integrator: (1) The INP001 per-file-ignore for `tests/**` must be added by whoever owns python_linting_practices_manifest.md (line 366). Until then the collection tells an agent to enable INP and mandates a test tree that trips it; my §1a states both resolutions, so nothing is wrong, but the config block is still incomplete. (2) My §8 bullet cites the hub's default as `requires-python = \">=3.13\"` and points at §3d–§3e. That number is attributed to the hub rather than asserted here, but it only reads correctly once the platform-baseline fixer has actually written the D1 default into §3e — check that landed. (3) The hub reference is §3d–§3e (verified both headings exist: §3d 'Th
====================================================================================================
FILES: error_tracing_contract_manifest.md, logging_observability_manifest.md
APPLIED: 7 | REFUTED: 2 | MEASURED: 10 | ADDED: 3
  REFUTED: Finding 1 (optional half): NOT applied — the prescription offers 'if a quotation from the standard is wanted, quote ISO 26262-1:2018's actual definition ("operating mode, in case of a failure of an item, without an unreasonable level of risk")'. I could not load the ISO OBP page (iso.org/obp is payw
  REFUTED: Finding 3 (out-of-scope half): NOT applied by me — 'Mirror that permitted escape in python_linting_practices_manifest.md §4.2 row for S101' belongs to that file's fixer. I applied only the error_tracing side; see notes.
  NOTES: All 5 findings + D6 (both files) + D1 (§21) applied; no finding in my file conflicted with DECISIONS.md.\n\nWhat changed in error_tracing_contract_manifest.md (7 edits):\n1. §6 error-kernel — de-quoted the element gloss, attributed to 'the `error-kernel` element's `what` field in `elements.json`, which carries no quotation provenance', and restored the full corpus wording (the old quote silently elided 'and critical logic' and the restart clause).\n2. Same line, second occurrence — the Armstrong edge quotation was the corpus's editorial `note` on `error-kernel -composes-with-> let-it-crash`; now reads '…citing Armstrong, *Programming Erlang*, and glossing it in the corpus's own note on that 
====================================================================================================
FILES: python_runtime_diagnostics_manifest.md, python_concurrency_determinism_manifest.md
APPLIED: 7 | REFUTED: 3 | MEASURED: 9 | ADDED: 9
  REFUTED: runtime_diagnostics finding 4, the prescribed added line 'On Windows the ladder is (b) py-spy only' — REFUTED. austin's own README compatibility table lists Windows x86_64/i686 (https://github.com/P403n1x87/austin, loaded 2026-08-08), so row (c) is available on Windows too; and rows (a)/(f) ride the
  REFUTED: runtime_diagnostics finding 2, the half of the prescription addressed to the hub ('retitle hub §6b to "the four things it does not", add the -X importtime/-X showrefcount and warnings items') — NOT APPLIED: python_platform_baseline_manifest.md is another fixer's file (DECISIONS.md D8 rule 4). I appl
  REFUTED: runtime_diagnostics finding 2, implicitly: the claim that all four -X dev non-effects are ESTABLISHED — PARTLY REFUTED against https://docs.python.org/3/library/devmode.html (loaded 2026-08-08), which states verbatim only two ('does not enable the tracemalloc module by default…' and 'does not preven
  NOTES: All 7 assigned findings closed. Scope notes for the integrator: (1) Beyond the 14 §10d–§10h references, I found and repointed SIX more mis-targeted pointers in the same file — §10c was cited for the maintenance-interface / _diagnostics.py mechanism, which actually lives in §10b's `maintenance-interface` row (former lines 404, 549, 565, and Open questions 4 and 5, and the sibling-manifests block). All §10b row references are now written uniformly as '§10b `row-name`'. (2) I also corrected §10b's heading from 'The eight mechanisms' to 'The nine mechanisms' (9 data rows, counted); the phrase appears nowhere else in the collection. (3) Finding 3's fix rippled: 'four hooks' → 'four channels' in t
====================================================================================================
FILES: README.md, python_typing_contract_manifest.md
APPLIED: 5 | REFUTED: 0 | MEASURED: 12 | ADDED: 9
  NOTES: No finding was refuted; all 5 (4 README + 1 typing) applied. Four deliberate deviations, all narrower or better-sourced than the prescription: (1) `runtime-catchable` placed AFTER `fitness-function`, not between `test-catchable` and `fitness-function` as the finding said, so README's row order matches the owning file's §0.1 order exactly. (2) The reading-order pointer reads `§2-§7`, not the prescribed `§2-§6`: the `[tool.ruff]` block is §4.3 and the formatter/linter division of labour is §6.1 and §7, so §2-§6 would cut the formatter half of the artefact the finding says is missing. (3) MEASURED is declared in the typing legend with 'none arise here' (the file's existing idiom for CC-FACT), b
====================================================================================================
TOTAL applied 66 refuted 24 measured 103
