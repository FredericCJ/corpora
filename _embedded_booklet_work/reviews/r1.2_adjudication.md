# r1.2 adversarial review — adjudication record

Three independent reviewers (real-time/concurrency; memory/failure/observability;
modularity/toolchain/verification), each chartered to refute, with "a well-evidenced refusal
to flag is success" stated in the charter. Every finding adjudicated here; accepted findings
applied in revision r1.2. Dispositions: ACCEPT (applied), ACCEPT-MOD (applied with changes,
noted), REJECT (not applied, reason recorded — dissent preserved per house rule G4).

## Reviewer A — real-time & concurrency (ch. 6–8)

| # | severity | finding (short) | disposition |
|---|---|---|---|
| A1 | CRITICAL | §7.3 SPSC: roles + atomic advancement stated as sufficient; missing forced visibility (qualified index access) and publication ordering (data-before-index; slot-copy-before-release). Single-core task→ISR direction breaks under compiler reordering. | ACCEPT — rewrite of the SPSC sentence with both obligations; contract clause added. |
| A2 | CRITICAL | §7.2 volatile: ordering hazard presented as multi-master-only; write buffers / posted bridges defer device stores on a single core (canonical spurious ISR re-entry after late source-clear). Also: volatile orders only among volatile accesses. | ACCEPT — partition corrected: completion + system ordering are barrier territory single-master included; read-back/barrier after source-clear named. |
| A3 | MAJOR | §7.3 double-buffer: bare two-bank swap does not guarantee a coherent snapshot with free-running writer; needs time control, handshake, or third bank. | ACCEPT — qualifier added; flip inherits A1's ordering obligations. |
| A4 | MINOR | §6.3 function-queue: "preemption's response-time benefits" overstates; bound is the longest queued function. | ACCEPT — bound stated. |
| A5 | MINOR | §7.1 "unanalyzable by the scheduler" contradicts §8.2's ISR-as-interference modeling. | ACCEPT — "outside the scheduler's control … appears only as interference". |
| A6 | MINOR | §8.2 PCP/RTA guarantees are uniprocessor fixed-priority theorems; unscoped claim would be false on SMP. | ACCEPT — single-scheduling-domain scoping clause added. |
| A7 | MINOR | §8.1 serial-number arithmetic: half-range condition omitted. | ACCEPT — width/resolution condition made part of the timestamp contract. |
| A8 | MINOR | §8.3 "every wait has a bound" proscribes the architecture's own idle queue-pend (§6.2) and invites timed-pend watchdog check-ins §8.4 forbids. | ACCEPT — "every wait within work"; the top-of-loop event wait exempted, owned by the watchdog map. |
| A9 | MINOR | §8.4 unconditional all-activities check-in starves the kick when activities are legitimately idle; gate on the mode-expected set. | ACCEPT — expected-active set made part of the check-in map. |
| A10 | MINOR | §6.2 "every interaction pays a copy" overstates; QP-lineage zero-copy pools + invariant 12's ownership transfer make the copy a per-design choice. | ACCEPT — envelope + hop as the floor; copy conditional on transfer contract. |

Reviewer A verdict as delivered: scheduling/watchdog/time canon sound; the two lock-free
mechanism claims were the substantive gaps.

## Reviewer B — memory, failure, observability (ch. 9–11)

| # | severity | finding (short) | disposition |
|---|---|---|---|
| B1 | MAJOR | Invariant 11 / §9.1: "post-init allocation failure is a design error" unscoped — exhaustion of pools provisioned against *external* arrivals is invariant 10's overflow case (shed-and-count), not an assert; the two invariants were in unacknowledged tension. | ACCEPT — scoped to design-controlled demand; external-arrival pools get a stated shed policy + counter, named as invariant 10's memory face. |
| B2 | MAJOR | §9.4 / invariant 13: watermark presented as measuring worst-case stack use; it is the achieved maximum — a floor. Worst case comes from static call-depth analysis where the graph permits (as §13.1 already said — internal inconsistency). | ACCEPT — ceiling from analysis (or measurement + named margin with residual risk on record); watermark demoted to in-service monitor. Invariant 13 reworded. |
| B3 | MAJOR | §9.5: log-structured shape "makes power-loss atomicity provable" — overstated; interrupted program/erase leaves marginal cells (CRC-valid on one read, not the next). Proof needs device model + read-back + commit mark + durable invalidation of a torn tail. | ACCEPT — "reduces … to the integrity of the last append; provable only against a stated device model", obligations named. |
| B4 | MINOR | §10.1: "entire failure surface visible in the signature" refuted by the section's own samurai disposition, traps, adapter side channels. | ACCEPT — scoped to the handleable failure surface. |
| B5 | MINOR | §10.1: saturation classified under absorb-*and-mark* but plain saturation leaves no mark — condemned by the section's own next sentence. | ACCEPT — absorb half only; honest when paired with sticky flag or counter. |
| B6 | MINOR | §10.5 × §11.1: startup RAM march destroys the reset-survivable region before harvest — unstated composition collision. | ACCEPT — ordering clause added: march spares the region or runs after harvest; the RAM map states which. |
| B7 | MINOR | §11.1: boot trusts/emits the persisted record unconditionally; power-on garbage pollutes evidence and a garbage escalation counter can park a healthy unit in safe-state. | ACCEPT — validity gate (magic + CRC) before trust; invalid region = "no record". |
| B8 | MINOR | §11.4: "other languages cannot deliver" removability — false uniqueness claim (C++/Rust/Ada feature-gate equally). | ACCEPT — comparative dropped. |

Reviewer B verdict as delivered: mechanisms and attributions solid; three absolutist claims
and five precision gaps, nothing invalidating the chapter architecture. Survivals recorded by
the reviewer (attacked, not refuted): allocation-ladder mechanics, ownership shapes, DMA
ownership + ping-pong, assertion lifetimes + blame, escalation/error-kernel/safe-state,
integrity kit, setjmp refusal, trace discipline, maintenance-surface rationale.

## Reviewer C — modularity, target decoupling, verification (ch. 3–5, 12–13)

| # | severity | finding (short) | disposition |
|---|---|---|---|
| C1 | MINOR | §3.1: "linker flag" cannot find a definition absent from headers — headers are erased before link; the finder is the require-declaration compiler-diagnostic class or an analysis rule. | ACCEPT. |
| C2 | MAJOR | §3.1/§3.3: include-path control stated as enforcement; quoted includes path around it and out-of-header `extern` bypasses headers entirely. Real gate = rung 4's resolved-dependency audit + analysis rule banning path-traversing includes and out-of-header externs. | ACCEPT — both sites reworded; rung 4 named as the gate. |
| C3 | MINOR | §3.2/invariant 2: "exactly one compiler-enforced hiding" inconsistent with internal linkage (§3.1's own private keyword). Unique property is representation-hiding with an exported handle. | ACCEPT — narrowed at both sites. |
| C4 | CRITICAL | §3.2: opaque storage type (caller-declared char-array storage cast to the real struct) violates effective-type rules — UB under the booklet's own §5.2 absolute; size/alignment static assert is necessary, not sufficient. | ACCEPT — hazard named; discharges added (memcpy in/out, union completion, or pinned aliasing guarantee recorded as a named extension reliance); otherwise prefer pool or published-struct. |
| C5 | MINOR | §3.2: pool resolution presented unpriced in a section demanding stated costs (count/placement migrate to module config; full footprint in every image). | ACCEPT — priced. |
| C6 | MINOR | §3.6: "greppable" wrong at identifier level — token-pasted products are invisible to text search; the table is greppable, the products are not. | ACCEPT — corrected with the predictable-pasting discipline. |
| C7 | MAJOR | §4.2/§12.4: unconditional cross-platform bit-determinism false for floating-point cores (evaluation method, contraction, libm accuracy, subnormal modes). | ACCEPT — conditioned: integer/fixed-point cores unconditional; FP cores require a pinned FP discipline in the platform contract, else replay is same-platform. |
| C8 | MINOR | §4.3/§5.4/§12.2: link-time binding "zero runtime cost" — zero indirection, but an optimizer barrier across the seam absent LTO; rungs 1 and 2 had identical prices, breaking the ladder's monotonicity. | ACCEPT. |
| C9 | MINOR | §4.3: function-pointer port cost list omits that indirect calls blind static stack/WCET analysis until target sets are enumerated. | ACCEPT — third cost added; ties to C15. |
| C10 | MAJOR | §5.2/§12.1/§13.1: "host build = second compiler, free ensemble" — same-family cross+native shares one frontend; ensemble requires deliberately differing family. | ACCEPT — all three sites: second *opinion* only if the family differs; make it differ. |
| C11 | MINOR | §5.5: MPU "enforces chapter 3's structure" — enforcement is protection-domain-granular (ownership/stacks/effect surfaces); module privacy within a domain stays with compiler+analyzers. | ACCEPT. |
| C12 | MINOR | §12.1: host-untestable residue list missing width-dependent implementation-defined semantics (promotions on 16-bit int targets). | ACCEPT. |
| C13 | MINOR | §12.1: "(§13.2)" points at the contract-only register; the ensemble claim lives in §13.1. | ACCEPT. |
| C14 | MINOR | §12.3: same-contract-suite absolute unmeetable — fault-injection cases cannot run against healthy silicon; partition nominal vs fault subsets per port. | ACCEPT. |
| C15 | MINOR | §13.1 rung 4/invariant 13: watermark trend is not a bound — analysis bounds, a trapped guard catches, the trend warns of drift. | ACCEPT — pairs with B2; both sites reworded. |

Reviewer C verdict as delivered: architecture sound, most C mechanics survive; one
UB-by-own-standard technique and three claims outrunning ISO C / toolchain reality; the rest
pricing omissions and quantifier overreach.

## Batch summary

33 findings: 3 CRITICAL (A1 SPSC sufficiency, A2 volatile/barrier partition, C4 opaque-storage
aliasing UB), 7 MAJOR, 23 MINOR. All 33 ACCEPTED; none rejected — each reviewer also recorded
attacked-and-survived claims rather than manufacturing findings. Applied in r1.2.
